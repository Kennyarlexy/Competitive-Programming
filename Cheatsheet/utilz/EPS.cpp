#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
double x, y;
const double EPS = 1e-10;
if (x + EPS < y) cout << "x < y\n";    // x < y
if (x <= EPS + y) cout << "x <= y\n";  // x <= y
    
    return 0;
}