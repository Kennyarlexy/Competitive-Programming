#include <bits/stdc++.h>
#define ll long long
using namespace std;

int Y[200005];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);.
    ll T; cin >> T;
    for (int t = 1; t <= T; t++) {
        int n; double d, h; cin >> n >> d >> h;
        for (int i = 1; i <= n; i++) {
            cin >> Y[i];
        }

        
    }
    
    return 0;
}