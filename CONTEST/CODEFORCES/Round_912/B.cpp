#include <bits/stdc++.h>
#define ll long long
using namespace std;

int M[1005][1005];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T; cin >> T;
    for (int t = 1; t <= T; t++) {
        int n; cin >> n;
        for (int r = 1; r <= n; r++) {
            for (int c = 1; c <= n; c++) {
                cin >> M[r][c];
            }
        }

        for (int i = 1; i <= n; i++) {
            
        }
    }

    
    
    return 0;
}