#include <bits/stdc++.h>
#define ll long long
#define fi first 
#define se second
using namespace std;

struct Node {
    int data;
    Node* next, prev;

    N  
};

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);


    
    return 0;
}